Skip to content
Navigation Menu
maheshmahi9597
devsec_demo

Type / to search
Code
Issues
Pull requests
Actions
Projects
Wiki
Security
Insights
Settings
Docker Image CI
Update docker-image.yml #3
Jobs
Run details
Workflow file for this run
.github/workflows/docker-image.yml at 5f0bf08
name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          registry: docker.io
          username: mahimani # Replace with your Docker Hub username
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Get current timestamp
        id: timestamp
        run: echo "TIMESTAMP=$(date +%s)" >> $GITHUB_OUTPUT

      - name: Build and push the Docker image
        id: docker_build
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile
          push: true
          tags: docker.io/mahimani/my-image-name:${{ steps.timestamp.outputs.TIMESTAMP }}
          #replace mahimani with your dockerhub username.
Update docker-image.yml · maheshmahi9597/devsec_demo@5f0bf08
